// Empty shell - will share the same component as UserMessages but just mounted here for routing. 
// Re-exporting UserMessages is perfectly fine since the API calls and logic handle roles seamlessly.
export { default } from '../user/Messages';